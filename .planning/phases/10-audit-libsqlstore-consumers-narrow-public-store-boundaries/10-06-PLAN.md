---
phase: 10-audit-libsqlstore-consumers-narrow-public-store-boundaries
plan: "06"
type: execute
wave: 6
depends_on:
  - "10-05"
files_modified:
  - src/scripts/doctor.ts
  - src/scripts/embed.ts
  - src/scripts/import.ts
  - src/tools/import.ts
  - src/ingest/workflow.ts
  - test/scripts/doctor.test.ts
  - test/scripts/embed.test.ts
  - test/ingest/workflow.test.ts
  - test/llama_embedder.test.ts
autonomous: true
requirements:
  - P10-02
  - P10-03
  - P10-04
  - P10-05
requirements_addressed:
  - P10-02
  - P10-03
  - P10-04
  - P10-05
must_haves:
  truths:
    - "Script/import internals prefer Effect runtime or branch-service execution over new Promise compatibility contracts."
    - "The Plan 10-02 `IngestionWorkflowStore` layer is reviewed and classified as public compatibility glue, not the target internal model."
    - "Default CLI paths still create, initialize, and dispose a public `LibSQLStore` through `createDefaultStore`."
    - "Helper tests prove direct service/runtime injection where practical, while integration tests keep real facade coverage."
  artifacts:
    - path: "src/ingest/workflow.ts"
      provides: "Reviewed workflow compatibility boundary and, if needed, Effect-first internal workflow helper"
      contains: "IngestionWorkflowStore"
    - path: "src/scripts/embed.ts"
      provides: "Script maintenance path that can run through direct runtime/branch services"
      contains: "embedStale"
    - path: "src/scripts/doctor.ts"
      provides: "Doctor path preserving CLI Promise boundary while avoiding unnecessary new store mirrors"
      contains: "runDoctor"
  key_links:
    - from: "src/scripts/import.ts"
      to: "src/ingest/workflow.ts"
      via: "public compatibility wrapper or Effect-first workflow helper"
      pattern: "createIngestionWorkflow\\("
---

<objective>
Move script/import internals to direct Effect runtime or branch services while preserving CLI/public Promise facades.

Purpose: Correct the old Plan 10-06 direction. The goal is not to mint `DoctorStore` / `EmbeddingMaintenanceStore` Promise mirrors; it is to use branch services directly where these scripts are internal, and leave Promise-shaped APIs only at executable CLI or exported compatibility boundaries.
Output: Script/workflow paths classified and migrated where practical, with tests proving runtime/service injection and facade preservation.
</objective>

<locked_direction>
Non-negotiable direction for this plan:
- Do not add new Promise compatibility interfaces in script/import lanes.
- Internal script and workflow behavior should execute through Effect runtime + branch services first.
- Keep Promise signatures only for public CLI/facade boundaries and mark them as compatibility wrappers.
</locked_direction>

<execution_context>
@D:/workspace/@yuyi919/external/whole-ends-kneel/packages/yui-agent/packages/brain-mastra/.codex/get-shit-done/workflows/execute-plan.md
@D:/workspace/@yuyi919/external/whole-ends-kneel/packages/yui-agent/packages/brain-mastra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-INVENTORY.md
@.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-CONTEXT.md
@.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-PATTERNS.md
@AGENTS.md
@docs/effect/v4-systematic-guide.md
@docs/effect/v4-banned-patterns.md
@src/scripts/doctor.ts
@src/scripts/embed.ts
@src/scripts/import.ts
@src/tools/import.ts
@src/ingest/workflow.ts
@src/store/index.ts
@src/store/BrainStore.ts
@test/scripts/doctor.test.ts
@test/scripts/embed.test.ts
@test/ingest/workflow.test.ts
@test/llama_embedder.test.ts
</context>

<interfaces>
Do not introduce `DoctorStore`, `EmbeddingMaintenanceStore`, or similar Promise mirror contracts unless a public exported helper truly needs them and they are documented as compatibility boundaries.

Preferred internal shape:
```typescript
export function someScriptEffect(...): Effect.Effect<Result, StoreError, BranchServiceA | BranchServiceB> {
  return Effect.gen(function* () {
    const service = yield* BranchServiceA;
    // branch calls here
  });
}
```
Public CLI wrappers may keep `async`/Promise signatures if they own process I/O and call `store.brainStore.runPromise(...)`.
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Review workflow/import compatibility and add direct Effect path where useful</name>
  <files>src/ingest/workflow.ts, src/scripts/import.ts, src/tools/import.ts, test/ingest/workflow.test.ts, test/llama_embedder.test.ts</files>
  <read_first>
    src/ingest/workflow.ts
    src/scripts/import.ts
    src/tools/import.ts
    src/store/BrainStore.ts
    test/ingest/workflow.test.ts
    test/llama_embedder.test.ts
  </read_first>
  <behavior>
    - Existing public `createIngestionWorkflow({ store, embedder })` remains compatible.
    - `IngestionWorkflowStore` is documented and tested as a compatibility boundary, not the internal target pattern.
    - If an Effect-first ingestion helper can be added with reasonable scope, it uses branch services directly and is covered by runtime/service tests.
  </behavior>
  <action>
    Review the Plan 10-02 workflow changes against D-23/D-24. If the workflow currently only exposes a Promise contract, either:
    - add an Effect-returning internal helper that takes parsed ingestion work and runs against `ContentPages`, `ContentChunks`, `GraphTimeline`, and related services, then make the public wrapper delegate through the runtime where available; or
    - if the full migration is too large for this phase, add explicit comments/tests/artifact notes that `IngestionWorkflowStore` is temporary compatibility glue and prevent later plans from treating it as the model.

    Keep `src/scripts/import.ts` and `src/tools/import.ts` public behavior stable. Do not require public callers to import branch services. Do not widen `StoreProvider`.
  </action>
  <verify>
    <automated>bun test test/ingest/workflow.test.ts test/llama_embedder.test.ts</automated>
    <automated>pwsh -NoProfile -Command "if (rg -n 'as unknown as StoreProvider|as any\\)\\.init|DoctorStore|EmbeddingMaintenanceStore' src/ingest src/scripts src/tools test/ingest) { throw 'old Promise-mirror or broad-cast pattern remains in workflow/import lane' }"</automated>
    <automated>rg -n "IngestionWorkflowStore|brainStore\\.runPromise|ContentPages|ContentChunks|GraphTimeline" src/ingest/workflow.ts src/scripts/import.ts test/ingest/workflow.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Workflow/import compatibility is explicitly bounded, and any new internal path uses Effect services instead of a new Promise mirror.
  </acceptance_criteria>
  <done>Workflow/import no longer presents the Plan 10-02 Promise layer as the internal target model.</done>
</task>

<task type="auto">
  <name>Task 2: Move doctor/embed script internals toward runtime services</name>
  <files>src/scripts/doctor.ts, src/scripts/embed.ts, test/scripts/doctor.test.ts, test/scripts/embed.test.ts</files>
  <read_first>
    src/scripts/doctor.ts
    src/scripts/embed.ts
    src/store/BrainStore.ts
    src/store/brainstore/ops/lifecycle/interface.ts
    src/store/brainstore/retrieval/embedding/interface.ts
    test/scripts/doctor.test.ts
    test/scripts/embed.test.ts
  </read_first>
  <action>
    Keep exported `runDoctor(...)` and `embedStale(...)` Promise signatures for CLI compatibility. Inside those wrappers, prefer runtime execution against branch services when the store provides `brainStore`.

    Do not create exported Promise interfaces such as `DoctorStore` or `EmbeddingMaintenanceStore`. If tests need injection, use `ManagedRuntime` with `Layer.succeed(...)` over the relevant branch service(s), or keep a real `LibSQLStore` integration path when the test intentionally verifies the facade/CLI boundary.

    Preserve owned default store behavior: when no store is injected, create the default `LibSQLStore`, initialize it, and dispose/clean up as before.
  </action>
  <verify>
    <automated>bun test test/scripts/doctor.test.ts test/scripts/embed.test.ts</automated>
    <automated>pwsh -NoProfile -Command "if (rg -n 'export interface DoctorStore|export interface EmbeddingMaintenanceStore|StoreProvider' src/scripts/doctor.ts src/scripts/embed.ts) { throw 'script lane still uses old Promise mirror or broad StoreProvider pattern' }"</automated>
    <automated>pwsh -NoProfile -Command "rg -n 'ManagedRuntime|Layer\\.succeed|brainStore\\.runPromise|new LibSQLStore' test/scripts src/scripts; if (rg -n 'store\\.vectorStore\\s*=' test/scripts) { throw 'raw vector mutation remains in script tests' }"</automated>
  </verify>
  <acceptance_criteria>
    Doctor/embed scripts keep CLI Promise facades but route internal work through runtime/branch services where practical, and tests prove service injection or intentional facade integration.
  </acceptance_criteria>
  <done>Script utilities are aligned with direct Effect runtime usage without breaking CLI behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/default factory to scripts | Scripts may still create and own public facade stores for command-line use. |
| runtime-backed script internals to branch services | Internal work should execute branch contracts through Effect runtime. |
| public workflow wrapper to internal ingestion behavior | Public `{ store, embedder }` remains compatibility glue. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-17 | Elevation of Privilege | script dependency shape | mitigate | Avoid new broad or Promise-mirror contracts; use branch service runtime execution for internal work. |
| T-10-18 | Tampering | `bulkImport` file traversal | mitigate | Preserve existing traversal/skip behavior while changing only dependency execution shape. |
| T-10-19 | Denial of Service | owned store disposal | mitigate | Keep owned default store disposal behavior and test cleanup under `./tmp/`. |
</threat_model>

<verification>
Run all task automated checks. Then run `pwsh ./scripts/check-effect-v4.ps1 src` if any Effect code is touched.
</verification>

<success_criteria>
P10-02 through P10-05 are satisfied for script/workflow consumers: public wrappers remain stable, internal paths do not rely on newly minted Promise compatibility layers, and helper tests prove runtime/service injection where practical.
</success_criteria>

<output>
After completion, create `.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-06-SUMMARY.md`.
</output>

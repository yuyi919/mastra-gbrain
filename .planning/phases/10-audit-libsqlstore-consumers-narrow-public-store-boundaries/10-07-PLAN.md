---
phase: 10-audit-libsqlstore-consumers-narrow-public-store-boundaries
plan: "07"
type: execute
wave: 7
depends_on:
  - "10-06"
files_modified:
  - test/libsql.test.ts
  - test/ext.test.ts
  - test/integration.test.ts
  - test/store_extensions.test.ts
  - .planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-FACADE-REGRESSION.md
autonomous: true
requirements:
  - P10-01
  - P10-02
  - P10-03
  - P10-04
  - P10-05
requirements_addressed:
  - P10-01
  - P10-02
  - P10-03
  - P10-04
  - P10-05
must_haves:
  truths:
    - "Public `LibSQLStore` and `StoreProvider` behavior is preserved."
    - "Facade tests intentionally remain on `LibSQLStore`."
    - "Static guards show broad internal dependencies have been narrowed or intentionally classified."
    - "Effect v4 and store implementation no-cast rules pass."
  artifacts:
    - path: ".planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-FACADE-REGRESSION.md"
      provides: "Final classification and regression evidence"
      contains: "P10-05"
    - path: "test/libsql.test.ts"
      provides: "Public facade compatibility lane"
      contains: "LibSQLStore"
  key_links:
    - from: "test/libsql.test.ts"
      to: "src/store/libsql.ts"
      via: "public Promise facade"
      pattern: "new LibSQLStore"
---

<objective>
Close Phase 10 with public facade regressions, static boundary guards, and a final evidence artifact.

Purpose: Ensure dependency narrowing did not erode public behavior, widen `StoreProvider`, or reintroduce banned Effect/store implementation patterns.
Output: `10-FACADE-REGRESSION.md` and updated facade/helper tests only if needed.
</objective>

<execution_context>
@D:/workspace/@yuyi919/external/whole-ends-kneel/packages/yui-agent/packages/brain-mastra/.codex/get-shit-done/workflows/execute-plan.md
@D:/workspace/@yuyi919/external/whole-ends-kneel/packages/yui-agent/packages/brain-mastra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-INVENTORY.md
@.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-VALIDATION.md
@.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-CONTEXT.md
@test/libsql.test.ts
@test/ext.test.ts
@test/integration.test.ts
@test/store_extensions.test.ts
@src/store/interface.ts
@src/store/libsql.ts
@src/store/index.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Preserve and label public facade regression lanes</name>
  <files>test/libsql.test.ts, test/ext.test.ts, test/integration.test.ts, test/store_extensions.test.ts</files>
  <read_first>
    .planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-INVENTORY.md
    .planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-CONTEXT.md
    test/libsql.test.ts
    test/ext.test.ts
    test/integration.test.ts
    test/store_extensions.test.ts
  </read_first>
  <action>
    Review facade tests against D-15 through D-17. Keep `test/libsql.test.ts` as public `LibSQLStore` compatibility coverage. Keep at least one real `LibSQLStore` path in `test/integration.test.ts` and `test/store_extensions.test.ts`. If `test/ext.test.ts` still contains helper-only raw vector mutations, move those helper-only assertions to narrow branch/provider seams from Plans 03 and 04, but keep public facade assertions that intentionally verify `LibSQLStore`.

    Add short comments only where useful to mark intentional public facade coverage; do not add comments to obvious tests. Do not delete public facade coverage to make grep counts smaller.
  </action>
  <verify>
    <automated>bun test test/libsql.test.ts test/ext.test.ts test/integration.test.ts test/store_extensions.test.ts</automated>
    <automated>rg -n "new LibSQLStore|LibSQLStore" test/libsql.test.ts test/ext.test.ts test/integration.test.ts test/store_extensions.test.ts</automated>
    <automated>pwsh -NoProfile -Command "if (rg -n 'store\\.vectorStore(\\s*=|\\.[A-Za-z_$][\\w$]*\\s*=)' test/ext.test.ts) { throw 'helper test still mutates public vectorStore or vectorStore methods instead of provider seam' }"</automated>
  </verify>
  <acceptance_criteria>
    Public facade tests remain on `LibSQLStore`; helper-only raw vector mutation is gone or explicitly isolated as facade behavior with a clear reason; all focused facade tests pass.
  </acceptance_criteria>
  <done>Public compatibility evidence remains while helper seams are narrowed.</done>
</task>

<task type="auto">
  <name>Task 2: Run final guards and record closure evidence</name>
  <files>.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-FACADE-REGRESSION.md</files>
  <read_first>
    .planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-INVENTORY.md
    .planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-VALIDATION.md
    src/store/interface.ts
    src/store/libsql.ts
    src/store/index.ts
    AGENTS.md
    docs/effect/v4-banned-patterns.md
  </read_first>
  <action>
    Create `10-FACADE-REGRESSION.md` with:
    - Requirement coverage table for P10-01 through P10-05.
    - Public API review notes for `src/store/interface.ts`, `src/store/libsql.ts`, and `src/store/index.ts`; state that `StoreProvider` was not widened for internal vector/chunk needs.
    - Guard grep results and interpretation. Public facade files may still match `LibSQLStore`, but internal workflow/tool/script/search files should not retain broad `StoreProvider` unless intentionally classified in `10-INVENTORY.md`.
    - Final test commands and pass/fail results.

    Do not modify production code in this task. If a guard fails, fix the owning earlier surface before writing closure evidence.
  </action>
  <verify>
    <automated>bun test</automated>
    <automated>pnpm exec tsc --noEmit</automated>
    <automated>pwsh ./scripts/check-effect-v4.ps1 src</automated>
    <automated>rg -n "LibSQLStore|new\\s+LibSQLStore" src/store/libsql.ts test/libsql.test.ts test/integration.test.ts test/store_extensions.test.ts</automated>
    <automated>rg -n "getChunksWithEmbeddings" src/store/brainstore/content/chunks src/store/brainstore/compat src/store/libsql.ts test/ext.test.ts</automated>
    <automated>pwsh -NoProfile -Command "if (rg -n 'StoreProvider' src/ingest src/tools src/scripts src/search test/ingest test/search) { throw 'unclassified broad StoreProvider remains in narrowed lanes' }"</automated>
    <automated>pwsh -NoProfile -Command "if (rg -n 'vectorStore\\?: LibSQLVector|deps\\.vectorStore|service\\.vectorStore|store\\.vectorStore(\\s*=|\\.[A-Za-z_$][\\w$]*\\s*=)' src/store/brainstore src/store/libsql-store.ts test) { throw 'raw vector dependency or helper vectorStore mutation remains' }"</automated>
    <automated>pwsh -NoProfile -Command "if (rg -n 'as unknown as StoreProvider|as any' src/store src/ingest src/workflow test/ingest test/search) { throw 'forbidden StoreProvider cast remains' }"</automated>
  </verify>
  <acceptance_criteria>
    `10-FACADE-REGRESSION.md` exists, records P10-01 through P10-05 closure evidence, full tests/typecheck/Effect guard pass, and no unclassified broad internal dependency remains.
  </acceptance_criteria>
  <done>Phase 10 has regression evidence that public behavior is preserved and consumer boundaries are narrowed.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public tests to public facade | Tests verify the Promise-facing `LibSQLStore` API. |
| static guards to source tree | Greps distinguish intentional public matches from internal boundary regressions. |
| final evidence to phase verification | Closure artifact feeds later `$gsd-verify-work` or audit review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-20 | Repudiation | Phase 10 regression evidence | mitigate | Record commands, requirement coverage, and guard interpretations in `10-FACADE-REGRESSION.md`. |
| T-10-21 | Tampering | public facade tests | mitigate | Keep `test/libsql.test.ts` and real integration lanes on `LibSQLStore`; do not remove facade coverage to satisfy internal narrowing. |
| T-10-22 | Elevation of Privilege | `StoreProvider` public API | mitigate | Review `src/store/interface.ts` and block public API widening for internal vector/chunk needs. |
| T-10-23 | Information Disclosure | raw vector access in tests | mitigate | Remove helper-only `store.vectorStore` mutation and require provider/branch seams for mocks. |
</threat_model>

<verification>
This is the phase gate: run all automated commands in Task 2 plus the focused facade tests in Task 1. Interpret broad grep matches by the `10-INVENTORY.md` classification instead of forcing public facade matches to zero.
</verification>

<success_criteria>
All Phase 10 requirements P10-01 through P10-05 are verified, public behavior is preserved, and internal consumers are narrowed or intentionally classified.
</success_criteria>

<output>
After completion, create `.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-07-SUMMARY.md`.
</output>

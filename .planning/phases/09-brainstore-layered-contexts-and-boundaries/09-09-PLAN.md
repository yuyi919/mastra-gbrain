---
phase: 09-brainstore-layered-contexts-and-boundaries
plan: "09"
type: execute
wave: 8
depends_on:
  - "08"
files_modified:
  - src/store/brainstore/compat/factory.ts
  - src/store/brainstore/compat/index.ts
  - src/store/libsql.ts
  - src/store/index.ts
  - test/libsql.test.ts
  - test/ingest/workflow.test.ts
autonomous: true
requirements:
  - P09-03
  - P09-04
must_haves:
  truths:
    - "The public Promise-facing adapter and provider still work after the tree migration because they now sit on a compat adapter layered over `BrainStoreTree`."
    - "No new public boundary is added to `StoreProvider` or `BrainStoreProvider` while compatibility is preserved."
  artifacts:
    - path: src/store/brainstore/compat/factory.ts
      provides: "Flat compat adapter derived from `BrainStoreTree`"
      contains: "BrainStore"
    - path: src/store/libsql.ts
      provides: "ManagedRuntime bridge over compat-over-tree"
      contains: "ManagedRuntime"
    - path: src/store/index.ts
      provides: "Stable provider wiring"
      contains: "BrainStoreProvider"
    - path: test/ingest/workflow.test.ts
      provides: "Workflow/provider compatibility regression"
      contains: "createIngestionWorkflow"
  key_links:
    - from: src/store/brainstore/compat/factory.ts
      to: src/store/brainstore/tree/factory.ts
      via: "compat-over-tree flattening"
      pattern: "BrainStoreTree"
    - from: src/store/libsql.ts
      to: src/store/brainstore/compat/factory.ts
      via: "ManagedRuntime bridge over compat adapter"
      pattern: "compat"
    - from: src/store/index.ts
      to: src/store/libsql.ts
      via: "stable BrainStoreProvider factory wiring"
      pattern: "BrainStoreProvider"
---

<objective>
Keep the public Promise and provider boundaries stable by rebuilding them on top of a compat adapter layered over `BrainStoreTree`.
</objective>

<context>
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-CONTEXT.md
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-RESEARCH.md
@src/store/interface.ts
@src/store/libsql.ts
@src/store/index.ts
@src/workflow/index.ts
@test/libsql.test.ts
@test/ingest/workflow.test.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the compat-over-tree adapter and route `libsql.ts` through it</name>
  <read_first>
    src/store/interface.ts
    src/store/libsql.ts
    src/store/brainstore/tree/factory.ts
  </read_first>
  <files>
    src/store/brainstore/compat/factory.ts
    src/store/brainstore/compat/index.ts
    src/store/libsql.ts
    test/libsql.test.ts
  </files>
  <action>
    Implement `compat/factory.ts` as a flat adapter derived from `BrainStoreTree`, then update `src/store/libsql.ts` so its `ManagedRuntime` resolves the compat layer over the tree runtime instead of assuming the flat root is the architecture center. Preserve all existing Promise-facing method signatures and verify them through `test/libsql.test.ts`.
  </action>
  <verify>
    <automated>bun test test/libsql.test.ts</automated>
    <automated>tsc --noEmit</automated>
    <automated>./scripts/check-effect-v4.sh</automated>
  </verify>
  <done>
    The Promise adapter now runs against compat-over-tree without widening public APIs.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep `BrainStoreProvider` and workflow wiring stable on the new runtime</name>
  <read_first>
    src/store/index.ts
    src/workflow/index.ts
    test/ingest/workflow.test.ts
  </read_first>
  <files>
    src/store/index.ts
    test/ingest/workflow.test.ts
  </files>
  <action>
    Update `src/store/index.ts` only enough to point the default provider wiring at the compat-backed runtime. Keep `BrainStoreProvider` thin, do not export internal branch tags here, and extend `test/ingest/workflow.test.ts` only enough to prove workflows still consume the unchanged `{ store, embedder }` provider surface.
  </action>
  <verify>
    <automated>bun test test/ingest/workflow.test.ts test/libsql.test.ts</automated>
    <automated>tsc --noEmit</automated>
    <automated>./scripts/check-effect-v4.sh</automated>
  </verify>
  <done>
    Provider and workflow boundaries remain stable on top of the new compat-over-tree runtime.
  </done>
</task>
</tasks>

<verification>
Wave 4 is complete only if both the LibSQL Promise adapter and workflow/provider wiring still pass against compat-over-tree.
</verification>


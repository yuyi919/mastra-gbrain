---
phase: 09-brainstore-layered-contexts-and-boundaries
plan: "07"
type: execute
wave: 6
depends_on:
  - "06"
files_modified:
  - src/store/brainstore/ops/lifecycle/factory.ts
  - src/store/brainstore/ops/lifecycle/index.ts
  - src/store/brainstore/ops/internal/factory.ts
  - src/store/brainstore/ops/internal/index.ts
  - src/store/brainstore/retrieval/search/factory.ts
  - src/store/brainstore/retrieval/search/index.ts
  - src/store/brainstore/retrieval/embedding/factory.ts
  - src/store/brainstore/retrieval/embedding/index.ts
  - test/store/brainstore-layers.test.ts
autonomous: true
requirements:
  - P09-02
  - P09-03
  - P09-04
must_haves:
  truths:
    - "Ops and retrieval behavior is implemented behind explicit small contracts, including lifecycle ownership, internal SQL fencing, backlink-count reads, and embedding lookups."
    - "A branch-only regression proves retrieval can run without requiring the full `BrainStore.Service` root."
  artifacts:
    - path: src/store/brainstore/ops/internal/factory.ts
      provides: "Internal runtime implementation over SQL, mappers, and vector handles"
      contains: "unsafe"
    - path: src/store/brainstore/retrieval/search/factory.ts
      provides: "Retrieval-search implementation over narrow collaborator contracts"
      contains: "getEmbeddingsByChunkIds"
    - path: src/store/brainstore/retrieval/embedding/factory.ts
      provides: "Embedding implementation owning vector and stale-helper behavior"
      contains: "getStaleChunks"
    - path: test/store/brainstore-layers.test.ts
      provides: "Passing branch-only injection regression"
      contains: "Layer.succeed"
  key_links:
    - from: src/store/brainstore/retrieval/search/factory.ts
      to: src/store/brainstore/graph/links/interface.ts
      via: "explicit backlink-count collaborator"
      pattern: "getBacklinkCounts"
    - from: src/store/brainstore/retrieval/search/factory.ts
      to: src/store/brainstore/retrieval/embedding/interface.ts
      via: "explicit embedding lookup collaborator"
      pattern: "getEmbeddingsByChunkIds"
    - from: src/store/brainstore/ops/internal/factory.ts
      to: src/store/brainstore/retrieval/embedding/factory.ts
      via: "internal vector and mapper access"
      pattern: "vector"
---

<objective>
Implement the ops and retrieval branches and prove the branch-only seam with a dedicated layer test.
</objective>

<context>
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-CONTEXT.md
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-RESEARCH.md
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-PATTERNS.md
@src/store/libsql-store.ts
@test/store/brainstore-layers.test.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement `ops.internal` and `ops.lifecycle` as the only homes for internal access and lifecycle</name>
  <read_first>
    src/store/libsql-store.ts
    src/store/Mappers.ts
    src/store/brainstore/ops/internal/interface.ts
    src/store/brainstore/ops/lifecycle/interface.ts
  </read_first>
  <files>
    src/store/brainstore/ops/internal/factory.ts
    src/store/brainstore/ops/internal/index.ts
    src/store/brainstore/ops/lifecycle/factory.ts
    src/store/brainstore/ops/lifecycle/index.ts
  </files>
  <action>
    Implement the internal and lifecycle branches using the existing logic from `src/store/libsql-store.ts`. `ops.internal` owns SQL, unsafe operations, mapper access, and vector handles; `ops.lifecycle` owns `init`, `dispose`, `transaction`, and resource-scoped acquisition. No other branch may reintroduce these behaviors ad hoc.
  </action>
  <verify>
    <automated>tsc --noEmit</automated>
    <automated>./scripts/check-effect-v4.sh</automated>
  </verify>
  <done>
    Internal runtime and lifecycle behavior are implemented behind the correct ops branches.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement retrieval over narrow collaborators and make the layer test pass</name>
  <read_first>
    src/store/libsql-store.ts
    src/search/hybrid.ts
    test/store/brainstore-layers.test.ts
  </read_first>
  <files>
    src/store/brainstore/retrieval/search/factory.ts
    src/store/brainstore/retrieval/search/index.ts
    src/store/brainstore/retrieval/embedding/factory.ts
    src/store/brainstore/retrieval/embedding/index.ts
    test/store/brainstore-layers.test.ts
  </files>
  <action>
    Implement `retrieval.search` and `retrieval.embedding` so retrieval consumes graph backlink counts and embedding lookups through explicit contracts only. Make `test/store/brainstore-layers.test.ts` pass as the proof that D-18 is real: retrieval can be injected with `Layer.succeed(...)` style collaborators and does not need the full `BrainStore.Service` root.
  </action>
  <verify>
    <automated>bun test test/store/brainstore-layers.test.ts</automated>
    <automated>tsc --noEmit</automated>
    <automated>./scripts/check-effect-v4.sh</automated>
  </verify>
  <done>
    Retrieval behavior is implemented over explicit collaborators, and the branch-only regression passes.
  </done>
</task>
</tasks>

<verification>
Wave 2 is not complete until the branch-only layer regression passes and repo-mandated type/Effect checks stay green.
</verification>


---
phase: 09-brainstore-layered-contexts-and-boundaries
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/brainstore/graph/links/interface.ts
  - src/store/brainstore/graph/links/factory.ts
  - src/store/brainstore/graph/links/index.ts
  - src/store/brainstore/graph/timeline/interface.ts
  - src/store/brainstore/graph/timeline/factory.ts
  - src/store/brainstore/graph/timeline/index.ts
autonomous: true
requirements:
  - P09-02
  - P09-04
must_haves:
  truths:
    - "The `graph.links` and `graph.timeline` branches exist independently under `src/store/brainstore/graph/**` and no longer require a broad root contract just to be named."
    - "Retrieval-facing backlink access is frozen as an explicit narrow graph contract instead of implicit sibling access through `BrainStore.Service`."
  artifacts:
    - path: src/store/brainstore/graph/links/interface.ts
      provides: "Link branch contract plus narrow backlink-count seam"
      contains: "getBacklinkCounts"
    - path: src/store/brainstore/graph/timeline/interface.ts
      provides: "Timeline branch contract"
      contains: "Context.Service"
  key_links:
    - from: src/store/brainstore/graph/links/index.ts
      to: src/store/brainstore/graph/links/interface.ts
      via: "re-exported backlink-count contract"
      pattern: "getBacklinkCounts"
    - from: src/store/brainstore/graph/timeline/index.ts
      to: src/store/brainstore/graph/timeline/interface.ts
      via: "re-exported branch contract"
      pattern: "interface"
---

<objective>
Create the graph branch folder skeletons and lock the backlink-count seam that later retrieval code must consume.
</objective>

<context>
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-CONTEXT.md
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-RESEARCH.md
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-PATTERNS.md
@src/store/libsql-store.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create `graph.links` with an explicit backlink-count contract</name>
  <read_first>
    src/store/libsql-store.ts
    .planning/phases/09-brainstore-layered-contexts-and-boundaries/09-PATTERNS.md
  </read_first>
  <files>
    src/store/brainstore/graph/links/interface.ts
    src/store/brainstore/graph/links/factory.ts
    src/store/brainstore/graph/links/index.ts
  </files>
  <action>
    Use the exact `src/store/libsql-store.ts` analog mapped in `09-PATTERNS.md` to create the `graph/links` folder. Freeze D-05 and D-06 directly into the design: export the full links contract for graph consumers, but also expose the narrow backlink-count style collaborator that `retrieval.search` will depend on later instead of reaching through a broad root.
  </action>
  <verify>
    <automated>rg -n "getBacklinkCounts|Context\\.Service|graph/links" src/store/brainstore/graph/links</automated>
    <automated>tsc --noEmit</automated>
    <automated>./scripts/check-effect-v4.sh</automated>
  </verify>
  <done>
    `graph.links` exists with a narrow retrieval-safe collaborator seam.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create the self-contained `graph.timeline` folder skeleton</name>
  <read_first>
    src/store/libsql-store.ts
    .planning/phases/09-brainstore-layered-contexts-and-boundaries/09-CONTEXT.md
  </read_first>
  <files>
    src/store/brainstore/graph/timeline/interface.ts
    src/store/brainstore/graph/timeline/factory.ts
    src/store/brainstore/graph/timeline/index.ts
  </files>
  <action>
    Create `graph/timeline` in the same `interface + factory + index` layout. Keep timeline focused on slug-to-page-id resolution and timeline CRUD only; do not let this branch grow hidden dependencies on unrelated content or retrieval features beyond explicit small contracts.
  </action>
  <verify>
    <automated>rg -n "Context\\.Service|makeLayer|graph/timeline" src/store/brainstore/graph/timeline</automated>
    <automated>tsc --noEmit</automated>
    <automated>./scripts/check-effect-v4.sh</automated>
  </verify>
  <done>
    `graph.timeline` is scaffolded and ready for later runtime implementation.
  </done>
</task>
</tasks>

<verification>
Wave 1 graph scaffolding is complete when the backlink-count seam exists and both graph folders compile as typed skeletons.
</verification>


---
context: phase
phase: 09-brainstore-layered-contexts-and-boundaries
task: 1
total_tasks: 20
status: paused
last_updated: 2026-04-25T19:05:54+08:00
---

# BLOCKING CONSTRAINTS - Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: PowerShell verify syntax - Phase planning and validation commands must not use bash-style `&&`; use PowerShell-safe sequencing with `; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }`.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Bash-style verification chaining in PowerShell | Checker failed because Phase 09 verification commands used `&&`, which is not valid in this shell context. | blocking | Write chained verify commands as PowerShell statements with explicit `$LASTEXITCODE` guards after each command. |
| Wrong-way absence checks | Checker failed because forbidden-pattern checks used plain `rg`, which succeeds when the bad pattern exists. | advisory | For “must be absent” checks, use `if (rg ...) { exit 1 }` so success means absence. |
| Wave/dependency mismatch in plan docs | Checker failed when dependent plans shared the same wave or contradicted the `depends_on` chain. | advisory | Keep `wave` monotonic with the dependency graph whenever plans are revised. |

<current_state>
Phase 09 planning is repaired and checker-passed. We are paused before execution starts. The active resume point is `09-01-PLAN.md`, Task 1, with no new Phase 09 implementation files created yet under `src/store/brainstore/**`.
</current_state>

<completed_work>

Completed Tasks:
- Session task: Repaired the full Phase 09 planning package - Done
- Session task: Split oversized plans into 10 smaller execution plans - Done
- Session task: Restored `09-VALIDATION.md`, resolved `09-RESEARCH.md` open questions, synced `ROADMAP.md` and `STATE.md` - Done
- Session task: Re-ran `gsd-plan-checker` until final `VERIFICATION PASSED` - Done
</completed_work>

<remaining_work>

- Start `09-01-PLAN.md` Task 1: create `src/store/brainstore/content/pages/interface.ts`, `factory.ts`, and `index.ts`
- Finish `09-01-PLAN.md` Task 2: create `content/chunks` skeleton without leaking embedding ownership
- Execute `09-02` through `09-10` in order, keeping tree-first + compat-over-tree boundaries intact
- Preserve existing public `StoreProvider` / `LibSQLStore` compatibility while moving implementation to branch-local layers
</remaining_work>

<decisions_made>

- Decided to center architecture on `BrainStoreTree` with the locked domain branches because that is the user-approved correction for the earlier flat-first refactor.
- Chose `compat-over-tree` instead of widening `StoreProvider` because internal architecture must change first while external boundaries stay stable.
- Chose per-branch `interface + factory + index` folders because the user explicitly requested on-disk structure to mirror the tree design.
- Chose PowerShell-safe verification commands plus `./scripts/check-effect-v4.sh` across Phase 09 because checker failures proved both requirements are enforced here.
</decisions_made>

<blockers>
- Dirty worktree: there are unrelated and user-owned modifications in `src/` and other planning areas. Status: not blocking execution, but resume work must not revert or accidentally absorb them into unrelated commits.
- No technical blocker remains in planning. The last checker run passed.
</blockers>

## Required Reading (in order)
1. [09-CONTEXT.md](D:/workspace/@yuyi919/external/whole-ends-kneel/packages/yui-agent/packages/brain-mastra/.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-CONTEXT.md) - locked user decisions and execution boundaries
1. [09-RESEARCH.md](D:/workspace/@yuyi919/external/whole-ends-kneel/packages/yui-agent/packages/brain-mastra/.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-RESEARCH.md) - resolved ownership decisions, migration order, and validation architecture
1. [09-PATTERNS.md](D:/workspace/@yuyi919/external/whole-ends-kneel/packages/yui-agent/packages/brain-mastra/.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-PATTERNS.md) - code analogs to follow for each branch
1. [09-VALIDATION.md](D:/workspace/@yuyi919/external/whole-ends-kneel/packages/yui-agent/packages/brain-mastra/.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-VALIDATION.md) - exact PowerShell-safe verification contract for this phase
1. [09-01-PLAN.md](D:/workspace/@yuyi919/external/whole-ends-kneel/packages/yui-agent/packages/brain-mastra/.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-01-PLAN.md) - immediate next execution target
1. [ROADMAP.md](D:/workspace/@yuyi919/external/whole-ends-kneel/packages/yui-agent/packages/brain-mastra/.planning/ROADMAP.md) - confirms Phase 09 is the active reopened phase with 10 plans
1. `.planning/METHODOLOGY.md` (if it exists) - project analytical lenses; apply before any assumption analysis

## Critical Anti-Patterns (do NOT repeat these)
- Re-introducing flat-root-first assembly through `BrainStore.use((store) => store.features.*)` -> preserve tree-first assembly and use compat only as a transitional adapter.
- Using bash shell idioms in Phase 09 verification commands -> keep all verify commands PowerShell-safe with explicit exit handling.
- Treating `retrieval.embedding` as optional or scattering vector helpers back into `content.chunks` -> keep vector/stale ownership centralized under the embedding branch.

## Infrastructure State
- Background processes: none started for this planning session
- Checker state: final `gsd-plan-checker` verdict was `VERIFICATION PASSED`
- Git state: dirty worktree with multiple unrelated source and planning changes; handoff commit should stay isolated from implementation work
- Resume context: no Phase 09 execution has started yet, so there is no partial branch skeleton to reconcile

## Pre-Execution Critique Required
- Design artifact: [09-01-PLAN.md](D:/workspace/@yuyi919/external/whole-ends-kneel/packages/yui-agent/packages/brain-mastra/.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-01-PLAN.md)
- Critique focus: confirm the first execution slice stays strictly within `content.pages` and `content.chunks`, does not leak vector ownership, and does not touch broad-root assembly prematurely
- Gate: Do NOT begin execution until the resuming agent has acknowledged the PowerShell verification constraint and the dirty-worktree isolation constraint

<context>
We paused at a good boundary. Planning is in a clean state now, and the next productive move is straightforward execution from 09-01. The main thing to protect on resume is discipline: work plan-by-plan, keep changes scoped, and do not let the existing dirty worktree or the old flat-root mental model pull the implementation off track.
</context>

<next_action>
Start with: read the four Phase 09 planning docs in order, then implement `09-01-PLAN.md` Task 1 by creating the `content/pages` branch skeleton under `src/store/brainstore/content/pages/` and verify it with the PowerShell-safe commands already encoded in the plan.
</next_action>

---
phase: 09-brainstore-layered-contexts-and-boundaries
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - src/store/BrainStore.ts
  - src/store/brainstore/retrieval/search/interface.ts
  - src/store/brainstore/retrieval/search/factory.ts
  - src/store/brainstore/retrieval/search/index.ts
  - src/store/brainstore/retrieval/embedding/interface.ts
  - src/store/brainstore/retrieval/embedding/factory.ts
  - src/store/brainstore/retrieval/embedding/index.ts
autonomous: true
requirements:
  - P09-01
  - P09-02
  - P09-04
must_haves:
  truths:
    - "`BrainStore.ts` becomes a transitional barrel over the new tree branches instead of the architectural center of the store."
    - "The retrieval contracts explicitly name the collaborator seams they need: backlink counts from graph and embedding reads/writes from the embedding branch."
  artifacts:
    - path: src/store/BrainStore.ts
      provides: "Transitional barrel re-exporting tree-oriented contracts"
      contains: "BrainStoreTree"
    - path: src/store/brainstore/retrieval/search/interface.ts
      provides: "Narrow retrieval-search contract matching `src/search/hybrid.ts`"
      contains: "getEmbeddingsByChunkIds"
    - path: src/store/brainstore/retrieval/embedding/interface.ts
      provides: "Embedding branch contract owning vector and stale-helper responsibilities"
      contains: "getStaleChunks"
  key_links:
    - from: src/store/brainstore/retrieval/search/interface.ts
      to: src/store/brainstore/graph/links/interface.ts
      via: "explicit backlink-count collaborator contract"
      pattern: "getBacklinkCounts"
    - from: src/store/brainstore/retrieval/search/interface.ts
      to: src/store/brainstore/retrieval/embedding/interface.ts
      via: "explicit embedding lookup collaborator contract"
      pattern: "getEmbeddingsByChunkIds"
    - from: src/store/BrainStore.ts
      to: src/store/brainstore/retrieval/search/index.ts
      via: "transitional barrel re-export"
      pattern: "BrainStoreSearch"
---

<objective>
Freeze the retrieval contracts and convert `src/store/BrainStore.ts` into a barrel that points toward `BrainStoreTree` instead of preserving flat-first architecture.
</objective>

<context>
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-CONTEXT.md
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-RESEARCH.md
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-PATTERNS.md
@src/store/BrainStore.ts
@src/search/hybrid.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Freeze `retrieval.search` around the already-proven narrow search seam</name>
  <read_first>
    src/search/hybrid.ts
    src/store/BrainStore.ts
    .planning/phases/09-brainstore-layered-contexts-and-boundaries/09-PATTERNS.md
  </read_first>
  <files>
    src/store/brainstore/retrieval/search/interface.ts
    src/store/brainstore/retrieval/search/factory.ts
    src/store/brainstore/retrieval/search/index.ts
  </files>
  <action>
    Create `retrieval/search` so it matches the narrow dependency surface already proven by `src/search/hybrid.ts`: `searchKeyword`, `searchVector`, `getBacklinkCounts`, and `getEmbeddingsByChunkIds`. Keep D-05, D-06, and D-18 explicit by naming collaborator contracts directly instead of planning to recover them from a broad root later.
  </action>
  <verify>
    <automated>rg -n "searchKeyword|searchVector|getBacklinkCounts|getEmbeddingsByChunkIds" src/store/brainstore/retrieval/search</automated>
    <automated>tsc --noEmit</automated>
    <automated>./scripts/check-effect-v4.sh</automated>
  </verify>
  <done>
    `retrieval.search` is frozen around the real narrow consumer seam.
  </done>
</task>

<task type="auto">
  <name>Task 2: Freeze `retrieval.embedding` ownership and rewrite `BrainStore.ts` as a transitional barrel</name>
  <read_first>
    src/store/libsql-store.ts
    src/store/BrainStore.ts
    .planning/phases/09-brainstore-layered-contexts-and-boundaries/09-CONTEXT.md
  </read_first>
  <files>
    src/store/BrainStore.ts
    src/store/brainstore/retrieval/embedding/interface.ts
    src/store/brainstore/retrieval/embedding/factory.ts
    src/store/brainstore/retrieval/embedding/index.ts
  </files>
  <action>
    Define `retrieval.embedding` as the owner of vector query/write, stale chunk helpers, and embedding lookup behavior, then update `src/store/BrainStore.ts` to re-export the new tree-oriented contracts while keeping the flat `BrainStore` root clearly transitional only per D-04, D-10, and D-11.
  </action>
  <verify>
    <automated>rg -n "BrainStoreTree|getStaleChunks|upsertVectors|markChunksEmbedded" src/store/BrainStore.ts src/store/brainstore/retrieval/embedding</automated>
    <automated>tsc --noEmit</automated>
    <automated>./scripts/check-effect-v4.sh</automated>
  </verify>
  <done>
    Retrieval ownership is frozen and `BrainStore.ts` now points toward tree-first assembly.
  </done>
</task>
</tasks>

<verification>
Wave 1 retrieval scaffolding is complete when the retrieval interfaces name only their explicit collaborators and `BrainStore.ts` advertises `BrainStoreTree` as the architectural center.
</verification>



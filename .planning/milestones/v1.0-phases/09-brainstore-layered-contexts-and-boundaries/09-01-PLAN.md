---
phase: 09-brainstore-layered-contexts-and-boundaries
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/brainstore/content/pages/interface.ts
  - src/store/brainstore/content/pages/factory.ts
  - src/store/brainstore/content/pages/index.ts
  - src/store/brainstore/content/chunks/interface.ts
  - src/store/brainstore/content/chunks/factory.ts
  - src/store/brainstore/content/chunks/index.ts
autonomous: true
requirements:
  - P09-01
  - P09-02
must_haves:
  truths:
    - "The `content.pages` and `content.chunks` branches exist as standalone contracts under `src/store/brainstore/content/**`, not as projections out of a flat root."
    - "The content branch folders already follow the locked `interface + factory + index` layout so later waves can implement them without redesigning the disk structure."
  artifacts:
    - path: src/store/brainstore/content/pages/interface.ts
      provides: "Page branch contract for the tree-first store"
      contains: "Context.Service"
    - path: src/store/brainstore/content/chunks/interface.ts
      provides: "Chunk branch contract for the tree-first store"
      contains: "Context.Service"
  key_links:
    - from: src/store/brainstore/content/pages/index.ts
      to: src/store/brainstore/content/pages/interface.ts
      via: "re-exported branch contract"
      pattern: "interface"
    - from: src/store/brainstore/content/chunks/index.ts
      to: src/store/brainstore/content/chunks/interface.ts
      via: "re-exported branch contract"
      pattern: "interface"
---

<objective>
Create the content branch folder skeleton so Phase 09 starts from the locked tree shape instead of the old flat-first store surface.
</objective>

<context>
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-CONTEXT.md
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-RESEARCH.md
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-PATTERNS.md
@src/store/BrainStore.ts
@src/store/libsql-store.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Freeze the `content.pages` branch contract and folder layout</name>
  <read_first>
    src/store/BrainStore.ts
    src/store/libsql-store.ts
    .planning/phases/09-brainstore-layered-contexts-and-boundaries/09-PATTERNS.md
  </read_first>
  <files>
    src/store/brainstore/content/pages/interface.ts
    src/store/brainstore/content/pages/factory.ts
    src/store/brainstore/content/pages/index.ts
  </files>
  <action>
    Following the `src/store/BrainStore.ts` and `src/store/libsql-store.ts` analogs from `09-PATTERNS.md`, create `content/pages` using `interface + factory + index` per D-03, D-13, D-14, and D-15. The files must define only the narrow page-facing contract and placeholder layer/factory surface needed for later implementation; do not import or depend on the broad `BrainStore` root.
  </action>
  <verify>
    <automated>rg -n "Context\\.Service|makeLayer|content/pages" src/store/brainstore/content/pages</automated>
    <automated>tsc --noEmit</automated>
    <automated>./scripts/check-effect-v4.sh</automated>
  </verify>
  <done>
    `content.pages` exists as a standalone branch contract ready for implementation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Freeze the `content.chunks` branch contract without vector leakage</name>
  <read_first>
    src/store/libsql-store.ts
    .planning/phases/09-brainstore-layered-contexts-and-boundaries/09-CONTEXT.md
    .planning/phases/09-brainstore-layered-contexts-and-boundaries/09-PATTERNS.md
  </read_first>
  <files>
    src/store/brainstore/content/chunks/interface.ts
    src/store/brainstore/content/chunks/factory.ts
    src/store/brainstore/content/chunks/index.ts
  </files>
  <action>
    Create `content/chunks` in the same folder pattern while enforcing D-05, D-06, D-08, and D-09: chunk CRUD and FTS responsibilities belong here, but vector write/read ownership must not leak back in through a broad root or through premature embedding methods. Keep the contract narrow and branch-local so later waves can connect retrieval through explicit small contracts only.
  </action>
  <verify>
    <automated>rg -n "Context\\.Service|makeLayer|content/chunks" src/store/brainstore/content/chunks</automated>
    <automated>tsc --noEmit</automated>
    <automated>./scripts/check-effect-v4.sh</automated>
  </verify>
  <done>
    `content.chunks` is scaffolded as an independent branch with the right future boundary.
  </done>
</task>
</tasks>

<verification>
Before leaving Wave 1, confirm the content branch folders exist and compile as typed skeletons.
</verification>


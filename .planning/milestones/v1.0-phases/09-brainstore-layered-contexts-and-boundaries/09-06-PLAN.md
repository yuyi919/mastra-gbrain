---
phase: 09-brainstore-layered-contexts-and-boundaries
plan: "06"
type: execute
wave: 5
depends_on:
  - "05"
files_modified:
  - src/store/brainstore/content/pages/factory.ts
  - src/store/brainstore/content/pages/index.ts
  - src/store/brainstore/content/chunks/factory.ts
  - src/store/brainstore/content/chunks/index.ts
  - src/store/brainstore/graph/links/factory.ts
  - src/store/brainstore/graph/links/index.ts
  - src/store/brainstore/graph/timeline/factory.ts
  - src/store/brainstore/graph/timeline/index.ts
autonomous: true
requirements:
  - P09-02
  - P09-03
must_haves:
  truths:
    - "Content and graph behavior is implemented in branch-local factories instead of living as anonymous object literals inside `src/store/libsql-store.ts`."
    - "The graph branch preserves the backlink-count seam that retrieval must consume without broad-root access."
  artifacts:
    - path: src/store/brainstore/content/pages/factory.ts
      provides: "Page branch implementation over the existing LibSQL behavior"
      contains: "putPage"
    - path: src/store/brainstore/content/chunks/factory.ts
      provides: "Chunk branch implementation over the existing LibSQL behavior"
      contains: "upsertChunks"
    - path: src/store/brainstore/graph/links/factory.ts
      provides: "Links branch implementation over the existing LibSQL behavior"
      contains: "getBacklinkCounts"
    - path: src/store/brainstore/graph/timeline/factory.ts
      provides: "Timeline branch implementation over the existing LibSQL behavior"
      contains: "addTimelineEntry"
  key_links:
    - from: src/store/brainstore/content/chunks/factory.ts
      to: src/store/brainstore/content/chunks/interface.ts
      via: "implemented chunk CRUD and FTS contract"
      pattern: "upsertChunks"
    - from: src/store/brainstore/graph/links/factory.ts
      to: src/store/brainstore/graph/links/interface.ts
      via: "implemented backlink-count contract"
      pattern: "getBacklinkCounts"
---

<objective>
Move content and graph behavior out of the old flat-root assembly file and into their branch-local factories.
</objective>

<context>
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-CONTEXT.md
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-RESEARCH.md
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-PATTERNS.md
@src/store/libsql-store.ts
@src/store/Mappers.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement `content.pages` and `content.chunks` from the current LibSQL sections</name>
  <read_first>
    src/store/libsql-store.ts
    src/store/brainstore/content/pages/interface.ts
    src/store/brainstore/content/chunks/interface.ts
  </read_first>
  <files>
    src/store/brainstore/content/pages/factory.ts
    src/store/brainstore/content/pages/index.ts
    src/store/brainstore/content/chunks/factory.ts
    src/store/brainstore/content/chunks/index.ts
  </files>
  <action>
    Move the existing page CRUD, versioning, chunk CRUD, and FTS maintenance logic from `src/store/libsql-store.ts` into the content branch factories while preserving D-17 behavior. `content.chunks` may depend on explicit internal contracts, but it must not silently re-own vector write/read behavior that belongs to `retrieval.embedding`.
  </action>
  <verify>
    <automated>tsc --noEmit</automated>
    <automated>./scripts/check-effect-v4.sh</automated>
  </verify>
  <done>
    Content behavior now lives behind branch-local factories.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `graph.links` and `graph.timeline` without broad-root access</name>
  <read_first>
    src/store/libsql-store.ts
    src/store/brainstore/graph/links/interface.ts
    src/store/brainstore/graph/timeline/interface.ts
  </read_first>
  <files>
    src/store/brainstore/graph/links/factory.ts
    src/store/brainstore/graph/links/index.ts
    src/store/brainstore/graph/timeline/factory.ts
    src/store/brainstore/graph/timeline/index.ts
  </files>
  <action>
    Move the current link CRUD, backlink counting, graph traversal, and timeline behavior into the graph branch factories. Keep D-05 and D-06 explicit: graph branches may use narrow collaborators, but they must not depend on `BrainStore.use((store) => store.features.*)` or any equivalent broad-root projection.
  </action>
  <verify>
    <automated>tsc --noEmit</automated>
    <automated>./scripts/check-effect-v4.sh</automated>
  </verify>
  <done>
    Graph behavior is implemented behind explicit branch factories and still preserves the retrieval-safe backlink seam.
  </done>
</task>
</tasks>

<verification>
This wave is complete when content and graph behavior has left the flat object assembly path and still passes repo-mandated type and Effect checks.
</verification>


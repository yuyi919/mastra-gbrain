---
phase: 09-brainstore-layered-contexts-and-boundaries
plan: "08"
type: execute
wave: 7
depends_on:
  - "07"
files_modified:
  - src/store/brainstore/tree/factory.ts
  - src/store/brainstore/tree/index.ts
  - src/store/libsql-store.ts
  - test/store/brainstore-tree.test.ts
autonomous: true
requirements:
  - P09-01
  - P09-02
  - P09-03
must_haves:
  truths:
    - "Branch layers are assembled into `BrainStoreTree` first, and the old flat-root projection path is removed from `src/store/libsql-store.ts`."
    - "The store runtime remains resource-scoped and does not duplicate lifecycle or vector ownership while switching to root-last assembly."
  artifacts:
    - path: src/store/brainstore/tree/factory.ts
      provides: "Root-last BrainStoreTree assembler"
      contains: "Layer.mergeAll"
    - path: src/store/libsql-store.ts
      provides: "LibSQL runtime wired through tree-first assembly"
      contains: "BrainStoreTree"
    - path: test/store/brainstore-tree.test.ts
      provides: "Passing tree-first assembly regression"
      contains: "compat"
  key_links:
    - from: src/store/libsql-store.ts
      to: src/store/brainstore/tree/factory.ts
      via: "tree-first runtime wiring"
      pattern: "BrainStoreTree"
    - from: test/store/brainstore-tree.test.ts
      to: src/store/brainstore/tree/factory.ts
      via: "root-last assembly regression"
      pattern: "compat"
---

<objective>
Assemble the real `BrainStoreTree` and remove the old flat-root projection path from the LibSQL runtime.
</objective>

<context>
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-CONTEXT.md
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-RESEARCH.md
@src/store/libsql-store.ts
@src/store/brainstore/tree/interface.ts
@test/store/brainstore-tree.test.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement `tree/factory.ts` as the only root assembler</name>
  <read_first>
    src/store/brainstore/tree/interface.ts
    src/store/libsql-store.ts
    .planning/phases/09-brainstore-layered-contexts-and-boundaries/09-PATTERNS.md
  </read_first>
  <files>
    src/store/brainstore/tree/factory.ts
    src/store/brainstore/tree/index.ts
  </files>
  <action>
    Implement `src/store/brainstore/tree/factory.ts` as the sole place that merges branch layers into a `BrainStoreTree`. Reuse the `Layer.mergeAll` composition idiom from the current store wiring, but invert the architecture so branches are built first and the root is derived last.
  </action>
  <verify>
    <automated>tsc --noEmit</automated>
    <automated>./scripts/check-effect-v4.sh</automated>
  </verify>
  <done>
    `BrainStoreTree` has one canonical root assembler.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rework `libsql-store.ts` to use tree-first runtime wiring and prove it with the tree test</name>
  <read_first>
    src/store/libsql-store.ts
    src/store/brainstore/tree/factory.ts
    test/store/brainstore-tree.test.ts
  </read_first>
  <files>
    src/store/libsql-store.ts
    test/store/brainstore-tree.test.ts
  </files>
  <action>
    Remove the `BrainStore.use((store) => Eff.succeed(store.features.*))` projection path from `src/store/libsql-store.ts`, wire the LibSQL runtime through `tree/factory.ts`, and update the tree regression so it proves the tree exists before any compat projection happens.
  </action>
  <verify>
    <automated>bun test test/store/brainstore-tree.test.ts</automated>
    <automated>if (rg -n "store\.features\.|BrainStore\.use\(\(store\) => Eff\.succeed\(store\.features" src/store/libsql-store.ts src/store/brainstore) { Write-Error "forbidden flat-root projection still present"; exit 1 }</automated>
    <automated>tsc --noEmit</automated>
    <automated>./scripts/check-effect-v4.sh</automated>
  </verify>
  <done>
    The runtime is tree-first, root-last, and no flat-root projection remains in production assembly.
  </done>
</task>
</tasks>

<verification>
Wave 3 is complete when tree-first assembly works, the tree regression passes, and the obsolete `store.features.*` projection path is gone.
</verification>



---
phase: 09-brainstore-layered-contexts-and-boundaries
plan: "04"
type: execute
wave: 3
depends_on:
  - "03"
files_modified:
  - src/store/brainstore/ops/lifecycle/interface.ts
  - src/store/brainstore/ops/lifecycle/factory.ts
  - src/store/brainstore/ops/lifecycle/index.ts
  - src/store/brainstore/ops/internal/interface.ts
  - src/store/brainstore/ops/internal/factory.ts
  - src/store/brainstore/ops/internal/index.ts
autonomous: true
requirements:
  - P09-03
must_haves:
  truths:
    - "Unsafe SQL, mapper access, vector resources, and low-level runtime handles are fenced behind `ops.internal` instead of leaking into public branches."
    - "Lifecycle operations remain centralized under `ops.lifecycle`, preserving a single owner for `init`, `dispose`, and `transaction`."
  artifacts:
    - path: src/store/brainstore/ops/internal/interface.ts
      provides: "Internal-only runtime and SQL contract"
      contains: "UnsafeDB"
    - path: src/store/brainstore/ops/lifecycle/interface.ts
      provides: "Lifecycle branch contract"
      contains: "transaction"
  key_links:
    - from: src/store/brainstore/ops/internal/index.ts
      to: src/store/brainstore/ops/internal/interface.ts
      via: "internal-only re-export"
      pattern: "UnsafeDB"
    - from: src/store/brainstore/ops/lifecycle/index.ts
      to: src/store/brainstore/ops/lifecycle/interface.ts
      via: "lifecycle branch re-export"
      pattern: "transaction"
---

<objective>
Create the fenced ops skeletons so later implementation work has one clear home for internal runtime access and one clear home for lifecycle behavior.
</objective>

<context>
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-CONTEXT.md
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-RESEARCH.md
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-PATTERNS.md
@src/store/libsql-store.ts
@src/store/Mappers.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Fence low-level SQL and runtime handles into `ops.internal`</name>
  <read_first>
    src/store/libsql-store.ts
    src/store/Mappers.ts
    src/libs/effect-drizzle/sqlite.ts
  </read_first>
  <files>
    src/store/brainstore/ops/internal/interface.ts
    src/store/brainstore/ops/internal/factory.ts
    src/store/brainstore/ops/internal/index.ts
  </files>
  <action>
    Create `ops/internal` as the only branch that names raw SQL, mapper access, vector resource handles, and unsafe runtime helpers. This task implements D-08 and D-09 structurally: later plans may depend on narrow internal contracts, but they must not widen `StoreProvider` or re-export unsafe capabilities through public compat surfaces.
  </action>
  <verify>
    <automated>rg -n "UnsafeDB|SqlClient|Mappers|vector" src/store/brainstore/ops/internal</automated>
    <automated>tsc --noEmit</automated>
    <automated>./scripts/check-effect-v4.sh</automated>
  </verify>
  <done>
    Internal runtime concerns have a single fenced home under `ops.internal`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Freeze `ops.lifecycle` as the sole owner of init/dispose/transaction</name>
  <read_first>
    src/store/libsql-store.ts
    .planning/phases/09-brainstore-layered-contexts-and-boundaries/09-CONTEXT.md
  </read_first>
  <files>
    src/store/brainstore/ops/lifecycle/interface.ts
    src/store/brainstore/ops/lifecycle/factory.ts
    src/store/brainstore/ops/lifecycle/index.ts
  </files>
  <action>
    Create `ops/lifecycle` with the current lifecycle responsibilities as its only target surface: `init`, `dispose`, `transaction`, and resource ownership. Do not let later branches recreate lifecycle behavior ad hoc; D-17 depends on keeping one canonical owner for these semantics during migration.
  </action>
  <verify>
    <automated>rg -n "init|dispose|transaction|acquireRelease" src/store/brainstore/ops/lifecycle</automated>
    <automated>tsc --noEmit</automated>
    <automated>./scripts/check-effect-v4.sh</automated>
  </verify>
  <done>
    Lifecycle semantics are frozen behind a dedicated branch boundary.
  </done>
</task>
</tasks>

<verification>
Wave 1 ops scaffolding is complete when unsafe/internal concerns and lifecycle concerns are separated into distinct folders and typed contracts.
</verification>



---
phase: 09-brainstore-layered-contexts-and-boundaries
plan: "10"
type: execute
wave: 9
depends_on:
  - "09"
files_modified:
  - src/search/hybrid.ts
  - test/search/hybrid.test.ts
  - test/ext.test.ts
  - test/libsql.test.ts
autonomous: true
requirements:
  - P09-04
must_haves:
  truths:
    - "Internal Effect consumers now depend on the smallest branch contracts they need instead of the compat root or the broad `BrainStore` root."
    - "Compatibility-sensitive embedding and stale-helper behavior still works through `LibSQLStore` after the tree migration."
  artifacts:
    - path: src/search/hybrid.ts
      provides: "Canonical narrowed retrieval consumer"
      contains: "BrainStoreSearch"
    - path: test/search/hybrid.test.ts
      provides: "Branch-only search injection regression"
      contains: "ManagedRuntime"
    - path: test/ext.test.ts
      provides: "Compat-backed embedding and stale-helper regression"
      contains: "getStaleChunks"
    - path: test/libsql.test.ts
      provides: "Compat-backed Promise surface regression"
      contains: "searchVector"
  key_links:
    - from: src/search/hybrid.ts
      to: src/store/brainstore/retrieval/search/interface.ts
      via: "narrow retrieval-branch dependency"
      pattern: "BrainStoreSearch"
    - from: test/ext.test.ts
      to: src/store/libsql.ts
      via: "compat-backed embedding and stale-helper regression"
      pattern: "getStaleChunks"
---

<objective>
Finish the migration by narrowing the remaining internal consumer and locking the phase with focused compatibility regressions.
</objective>

<context>
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-CONTEXT.md
@.planning/phases/09-brainstore-layered-contexts-and-boundaries/09-RESEARCH.md
@src/search/hybrid.ts
@src/store/libsql.ts
@test/search/hybrid.test.ts
@test/ext.test.ts
@test/libsql.test.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Make `src/search/hybrid.ts` the canonical narrow retrieval consumer</name>
  <read_first>
    src/search/hybrid.ts
    src/store/brainstore/retrieval/search/interface.ts
    test/search/hybrid.test.ts
  </read_first>
  <files>
    src/search/hybrid.ts
    test/search/hybrid.test.ts
  </files>
  <action>
    Audit `src/search/hybrid.ts` for any remaining conceptual reliance on the old flat root and make it depend only on the narrow retrieval-search contract. Keep the existing `Layer.succeed(...)` injection style working in `test/search/hybrid.test.ts` so this module remains the clearest proof of D-18 in real code.
  </action>
  <verify>
    <automated>bun test test/search/hybrid.test.ts</automated>
    <automated>tsc --noEmit</automated>
    <automated>./scripts/check-effect-v4.sh</automated>
  </verify>
  <done>
    Search is fully aligned to the narrow retrieval branch interface.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock Phase 09 with compat-backed embedding and stale-helper regressions</name>
  <read_first>
    test/ext.test.ts
    test/libsql.test.ts
    src/store/libsql.ts
  </read_first>
  <files>
    test/ext.test.ts
    test/libsql.test.ts
  </files>
  <action>
    Tighten the compatibility regressions around `searchVector`, `getEmbeddingsByChunkIds`, `getStaleChunks`, `markChunksEmbedded`, and `upsertVectors`, all through the public `LibSQLStore` boundary. Close the phase by also asserting that production store assembly no longer depends on `store.features.*` projection anywhere under `src/store`.
  </action>
  <verify>
    <automated>bun test test/search/hybrid.test.ts test/ext.test.ts test/libsql.test.ts</automated>
    <automated>if (rg -n "store\.features\.|BrainStore\.use\(\(store\) => Eff\.succeed\(store\.features" src/store) { Write-Error "forbidden flat-root projection still present"; exit 1 }</automated>
    <automated>tsc --noEmit</automated>
    <automated>./scripts/check-effect-v4.sh</automated>
  </verify>
  <done>
    Phase 09 has both narrow-consumer proof and compat-backed regression coverage for the riskiest store behaviors.
  </done>
</task>
</tasks>

<verification>
Phase 09 exits only when branch-only search injection, compat-backed embedding helpers, typecheck, and the Effect v4 guard script all pass together.
</verification>



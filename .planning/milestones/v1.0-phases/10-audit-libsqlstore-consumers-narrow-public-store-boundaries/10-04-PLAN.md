---
phase: 10-audit-libsqlstore-consumers-narrow-public-store-boundaries
plan: "04"
type: execute
wave: 4
depends_on:
  - "10-03"
files_modified:
  - src/store/brainstore/content/chunks/interface.ts
  - src/store/brainstore/content/chunks/factory.ts
  - src/store/brainstore/tree/interface.ts
  - src/store/brainstore/compat/factory.ts
  - src/store/BrainStore.ts
  - src/store/libsql-store.ts
  - src/store/libsql.ts
  - test/ext.test.ts
  - test/store/brainstore-layers.test.ts
autonomous: true
requirements:
  - P10-02
  - P10-04
  - P10-05
requirements_addressed:
  - P10-02
  - P10-04
  - P10-05
must_haves:
  truths:
    - "`getChunksWithEmbeddings(slug)` is owned by the content chunks branch."
    - "`LibSQLStore.getChunksWithEmbeddings(slug)` still works through the Promise facade."
    - "Tests prove branch/provider access instead of reaching through raw vector internals for this helper."
  artifacts:
    - path: "src/store/brainstore/content/chunks/interface.ts"
      provides: "Branch-owned chunk embedding lookup capability"
      contains: "getChunksWithEmbeddings"
    - path: "src/store/brainstore/compat/factory.ts"
      provides: "Compat projection of branch-owned chunks helper"
      contains: "tree.content.chunks.getChunksWithEmbeddings"
  key_links:
    - from: "src/store/libsql.ts"
      to: "src/store/brainstore/compat/factory.ts"
      via: "BrainStoreCompat facade method"
      pattern: "getChunksWithEmbeddings"
---

<objective>
Move `getChunksWithEmbeddings` into the content chunks branch and keep the public facade projection intact.

Purpose: Implement D-12 and D-21 without widening `StoreProvider` or making `libsql-store.ts` reimplement feature branch behavior.
Output: Branch-owned chunk helper plus facade/compat regression coverage.

Correction note: this plan remains aligned with D-23/D-24 because it moves ownership into an Effect branch service. Do not add a Promise-shaped helper contract except for the existing public `LibSQLStore` facade projection.
</objective>

<execution_context>
@D:/workspace/@yuyi919/external/whole-ends-kneel/packages/yui-agent/packages/brain-mastra/.codex/get-shit-done/workflows/execute-plan.md
@D:/workspace/@yuyi919/external/whole-ends-kneel/packages/yui-agent/packages/brain-mastra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-INVENTORY.md
@.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-PATTERNS.md
@.planning/todos/pending/2026-04-25-refine-phase-9-content-chunks-and-vector-provider-layers.md
@AGENTS.md
@docs/effect/v4-systematic-guide.md
@src/store/brainstore/content/chunks/interface.ts
@src/store/brainstore/content/chunks/factory.ts
@src/store/brainstore/compat/factory.ts
@src/store/BrainStore.ts
@src/store/libsql-store.ts
@src/store/libsql.ts
@test/ext.test.ts
</context>

<interfaces>
Add this branch method to `ContentChunksService`:
```typescript
getChunksWithEmbeddings(slug: string): EngineEffect<Chunk[]>;
```
For this phase, preserve current public behavior: return the same chunk shape currently returned by the legacy compat helper unless existing tests require actual vector payloads. Do not add a new public `StoreProvider` method.
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add branch-owned chunks helper</name>
  <files>src/store/brainstore/content/chunks/interface.ts, src/store/brainstore/content/chunks/factory.ts, test/store/brainstore-layers.test.ts</files>
  <read_first>
    src/store/brainstore/content/chunks/interface.ts
    src/store/brainstore/content/chunks/factory.ts
    src/store/brainstore/content/pages/interface.ts
    test/store/brainstore-layers.test.ts
    docs/effect/v4-systematic-guide.md
  </read_first>
  <behavior>
    - `ContentChunksService` exposes `getChunksWithEmbeddings`.
    - `makeContentChunks` implements the method by delegating to branch-owned chunk reads while preserving current return shape.
    - Branch-only tests can inject or run `ContentChunks` with the new method through an Effect runtime without using `LibSQLStore`.
  </behavior>
  <action>
    In `src/store/brainstore/content/chunks/interface.ts`, add `getChunksWithEmbeddings(slug: string): EngineEffect<Chunk[]>` to `ContentChunksService` per D-12.

    In `src/store/brainstore/content/chunks/factory.ts`, implement the method with `Eff.fn("content.chunks.getChunksWithEmbeddings")` and delegate to the branch's existing `getChunks(slug)` implementation unless existing tests require richer embedding payloads. Keep error handling through `StoreError.catch`. Do not introduce raw vector provider usage here unless the current contract already needs it.

    Update `test/store/brainstore-layers.test.ts` so any `ContentChunks.of({ ... })` mock includes `getChunksWithEmbeddings`, and prefer `ManagedRuntime` / `Layer.succeed(ContentChunks, ...)` proof over a Promise facade mock.
  </action>
  <verify>
    <automated>bun test test/store/brainstore-layers.test.ts</automated>
    <automated>pwsh ./scripts/check-effect-v4.ps1 src</automated>
  </verify>
  <acceptance_criteria>
    `ContentChunksService` and `makeContentChunks` both contain `getChunksWithEmbeddings`, and branch tests run through Effect service injection without a `LibSQLStore` instance for this method.
  </acceptance_criteria>
  <done>The helper is branch-owned by content chunks.</done>
</task>

<task type="auto">
  <name>Task 2: Project branch helper through compat and public facade</name>
  <files>src/store/brainstore/tree/interface.ts, src/store/brainstore/compat/factory.ts, src/store/BrainStore.ts, src/store/libsql-store.ts, src/store/libsql.ts, test/ext.test.ts</files>
  <read_first>
    src/store/brainstore/tree/interface.ts
    src/store/brainstore/compat/factory.ts
    src/store/BrainStore.ts
    src/store/libsql-store.ts
    src/store/libsql.ts
    test/ext.test.ts
    .planning/phases/09-brainstore-layered-contexts-and-boundaries/09-09-SUMMARY.md
  </read_first>
  <action>
    Route the existing public `LibSQLStore.getChunksWithEmbeddings(slug)` through the branch-owned method:
    - Ensure `BrainStoreTreeService.content.chunks` includes the updated `ContentChunksService`.
    - In `src/store/brainstore/compat/factory.ts`, implement the compat helper by calling `tree.content.chunks.getChunksWithEmbeddings(slug)`.
    - In `src/store/BrainStore.ts`, keep only aliases/compositions for compatibility; do not redeclare a separate feature contract.
    - In `src/store/libsql-store.ts`, remove any local `brainstore.compat.getChunksWithEmbeddings` implementation or manual branch reconstruction. The file should compose layers only.
    - In `src/store/libsql.ts`, keep the public Promise method unchanged.

    Update `test/ext.test.ts` so the public facade assertion for `getChunksWithEmbeddings` remains, but any helper-only proof prefers branch/provider seams added in Task 1.
  </action>
  <verify>
    <automated>bun test test/ext.test.ts test/libsql.test.ts test/store/brainstore-layers.test.ts</automated>
    <automated>pwsh -NoProfile -Command "if (rg -n 'brainstore\\.compat\\.getChunksWithEmbeddings|makeCompat.*getChunksWithEmbeddings' src/store/libsql-store.ts) { throw 'compat helper still implemented in libsql-store.ts' }; rg -n 'getChunksWithEmbeddings' src/store/brainstore/content/chunks src/store/brainstore/compat src/store/libsql.ts test/ext.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    `src/store/libsql-store.ts` no longer owns helper behavior; `src/store/brainstore/compat/factory.ts` projects from `tree.content.chunks.getChunksWithEmbeddings`; public facade tests still pass.
  </acceptance_criteria>
  <done>`getChunksWithEmbeddings` ownership is moved out of compat-only assembly while preserving `LibSQLStore` behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content branch to compat facade | Branch-owned chunk reads are projected through compatibility services. |
| public facade to branch helper | Promise callers still use `LibSQLStore`, while implementation lives in branch code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Tampering | `getChunksWithEmbeddings` behavior | mitigate | Preserve current return shape and verify through `test/ext.test.ts` and `test/libsql.test.ts`. |
| T-10-12 | Elevation of Privilege | `libsql-store.ts` compat assembly | mitigate | Remove local helper behavior from `libsql-store.ts`; project from `ContentChunksService` only. |
| T-10-13 | Information Disclosure | chunk/vector boundary | mitigate | Do not expose raw vector payloads unless existing public tests already require them; keep vector provider internal. |
</threat_model>

<verification>
Run focused tests and guard greps from both tasks. If actual embedding payloads are required by a failing public test, implement them through the typed vector provider from Plan 03, not by reintroducing raw vector access.
</verification>

<success_criteria>
P10-02, P10-04, and P10-05 are satisfied for chunk helper ownership: branch code owns the helper, compat projects it, and public facade behavior remains stable.
</success_criteria>

<output>
After completion, create `.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-04-SUMMARY.md`.
</output>

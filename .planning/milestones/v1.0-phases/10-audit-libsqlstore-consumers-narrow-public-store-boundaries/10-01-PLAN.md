---
phase: 10-audit-libsqlstore-consumers-narrow-public-store-boundaries
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-INVENTORY.md
autonomous: true
requirements:
  - P10-01
requirements_addressed:
  - P10-01
must_haves:
  truths:
    - "Every current LibSQLStore, StoreProvider, BrainStore, vectorStore, and getChunksWithEmbeddings reference in src/** and test/** is inventoried before implementation changes."
    - "Each inventory row has one required classification: public facade coverage, provider wiring, workflow/tool consumer, script utility, or replaceable internal dependency."
    - "Later plans have a frozen artifact to read before changing consumer boundaries."
  artifacts:
    - path: ".planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-INVENTORY.md"
      provides: "Frozen Phase 10 consumer inventory and migration order"
      contains: "public facade coverage"
  key_links:
    - from: ".planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-INVENTORY.md"
      to: "src/** and test/**"
      via: "rg inventory commands"
      pattern: "LibSQLStore|StoreProvider|BrainStore|vectorStore|getChunksWithEmbeddings"
---

<objective>
Freeze the Phase 10 consumer inventory before any implementation changes.

Purpose: Locked decisions D-07 and D-08 require an audit-first refactor. This plan creates the artifact that later executor contexts must read before narrowing workflow, vector, tool, script, or test dependencies.
Output: `.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-INVENTORY.md`
</objective>

<execution_context>
@D:/workspace/@yuyi919/external/whole-ends-kneel/packages/yui-agent/packages/brain-mastra/.codex/get-shit-done/workflows/execute-plan.md
@D:/workspace/@yuyi919/external/whole-ends-kneel/packages/yui-agent/packages/brain-mastra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-CONTEXT.md
@.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-RESEARCH.md
@.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-PATTERNS.md
@.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-VALIDATION.md
@AGENTS.md
@docs/effect/v4-systematic-guide.md
@docs/effect/v4-playbook.md
@docs/effect/v4-banned-patterns.md
@docs/effect/effect-v4-agent-skill.md
</context>

<source_audit>
SOURCE | ID | Requirement or decision | Plan | Status | Notes
GOAL | phase-goal | Audit every current `LibSQLStore` reference, narrow internal modules to stable capability contracts, and keep `LibSQLStore` as deliberate Promise facade. | 01-07 | COVERED | Inventory, workflow, vector/chunks, tools/scripts, and facade regression are all planned.
REQ | P10-01 | Inventory and classify every current `LibSQLStore` import/constructor usage across `src/**` and `test/**`. | 01 | COVERED | This plan creates the frozen inventory.
REQ | P10-02 | Replace broad `LibSQLStore`/`BrainStore` dependencies with capability-specific contracts where callers do not need the full facade. | 02,05,06 | COVERED | Workflow, tool, search, script, and helper test contracts are narrowed.
REQ | P10-03 | Stabilize provider and ingestion workflow surface around `{ store, embedder }`. | 02 | COVERED | `createIngestionWorkflow` keeps caller shape and narrows `store`.
REQ | P10-04 | Keep public facade tests and convert internal/helper tests where better. | 04,05,06,07 | COVERED | Public lane remains while branch/provider lanes are added.
REQ | P10-05 | Preserve public `StoreProvider` and `LibSQLStore` behavior; do not widen public API. | 03,04,07 | COVERED | Vector/chunk changes remain internal or compat-projected, then final regressions verify facade behavior.
RESEARCH | inventory-first order | Freeze inventory before edits. | 01 | COVERED | Locked by D-07.
RESEARCH | workflow narrow store | Use `IngestionWorkflowStore` with page lookup, page/version/tag/chunk writes, optional transaction, and timeline batch writes. | 02 | COVERED | D-04 through D-06.
RESEARCH | vector boundary | Move raw vector operations behind typed provider and move `getChunksWithEmbeddings` into content chunks. | 03,04 | COVERED | D-11 through D-14 and D-21.
RESEARCH | tool/script narrowing | Tool and script factories should accept narrower Promise contracts while public wiring remains structural. | 05,06 | COVERED | D-09 and D-10.
RESEARCH | validation guards | Run focused tests, full suite, Effect v4 guard, and classified greps. | 01-07 | COVERED | Each plan has automated verification.
CONTEXT | D-01-D-03 | Keep `LibSQLStore` as public Promise facade/provider default and preserve behavior. | 07 | COVERED | Facade tests remain and public API widening is blocked.
CONTEXT | D-04-D-06 | Keep workflow `{ store, embedder }` and do not expose branches to workflow callers. | 02 | COVERED | `IngestionWorkflowStore` is local workflow contract.
CONTEXT | D-07-D-08 | Inventory and classify before implementation. | 01 | COVERED | This plan is wave 1 and has no code edits.
CONTEXT | D-09-D-10 | Narrow tools/scripts where practical and follow `src/search/hybrid.ts` narrow model. | 05,06 | COVERED | Shared tool contracts and search promise backend planned.
CONTEXT | D-11-D-14, D-21 | Fold vector-boundary todo, branch-own `getChunksWithEmbeddings`, and keep raw vector internal/facade-only. | 03,04 | COVERED | Vector provider and chunks ownership planned before helper test migration.
CONTEXT | D-15-D-17 | Keep facade tests and convert internal/helper tests to narrower seams where better. | 04-07 | COVERED | Test lane classification is part of later plans.
CONTEXT | D-18-D-20 | Follow Effect v4, avoid `as unknown`/`as any` in store implementation files, and keep `libsql-store.ts` as assembly boundary. | 03,04,07 | COVERED | Effect guard and store grep guards required.
</source_audit>

<tasks>
<task type="auto">
  <name>Task 1: Create the frozen consumer inventory</name>
  <files>.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-INVENTORY.md</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-CONTEXT.md
    .planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-RESEARCH.md
    .planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-PATTERNS.md
    .planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-VALIDATION.md
    AGENTS.md
  </read_first>
  <action>
    Create `10-INVENTORY.md` before modifying any `src/**` or `test/**` files. Include:
    1. The exact commands used:
       - `rg -n "LibSQLStore|new\s+LibSQLStore|StoreProvider|BrainStore|vectorStore|getChunksWithEmbeddings" src test`
       - `rg -n "StoreProvider|EmbeddingProvider|bulkImport|embedStale|runDoctor|createIngestionWorkflow" src/ingest src/workflow src/tools src/scripts src/agent src/search test/ingest test/search test/scripts`
       - `rg -n "vectorStore|getChunksWithEmbeddings|makeLayer|Context\.Service" src/store/BrainStore.ts src/store/libsql-store.ts src/store/libsql.ts src/store/brainstore`
    2. A table with columns `File`, `Symbol`, `Usage`, `Classification`, `Phase 10 action`, and `Decision refs`.
    3. One row for every current match in `src/**` and `test/**`; classify each as exactly one of: `public facade coverage`, `provider wiring`, `workflow/tool consumer`, `script utility`, or `replaceable internal dependency`.
    4. A "Do not migrate" section that names intentional public facade lanes: `src/store/libsql.ts`, `src/store/index.ts`, `test/libsql.test.ts`, `test/integration.test.ts`, and `test/store_extensions.test.ts`.
    5. A "Migration order" section matching the plan set: workflow/provider, vector provider, chunks ownership, tools/search, scripts/helper tests, facade closure.
    Reference D-07 and D-08 beside the inventory precondition.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "if (!(Test-Path '.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-INVENTORY.md')) { throw 'missing inventory' }; rg -n 'public facade coverage|provider wiring|workflow/tool consumer|script utility|replaceable internal dependency|D-07|D-08' .planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-INVENTORY.md; rg -n 'LibSQLStore|new\s+LibSQLStore|StoreProvider|BrainStore|vectorStore|getChunksWithEmbeddings' src test"</automated>
  </verify>
  <acceptance_criteria>
    The inventory file exists, contains all five classification labels, references D-07 and D-08, lists the exact guard grep commands, and explicitly states that implementation changes are blocked until this file is complete.
  </acceptance_criteria>
  <done>Every current consumer is classified and later implementation plans can cite `10-INVENTORY.md` as the frozen source of truth for P10-01.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner-to-executor | Inventory omissions can cause executors to change consumers without understanding whether they are public facade coverage or internal dependencies. |
| public-facade-to-internal-store | Misclassification can accidentally remove compatibility evidence or expose internal store capabilities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `10-INVENTORY.md` | mitigate | Require exact grep commands, all five classifications, and decision references D-07/D-08 before implementation plans run. |
| T-10-02 | Information Disclosure | Store boundary classification | mitigate | Inventory must separate public facade coverage from replaceable internals so raw SQL/vector surfaces are not promoted by mistake. |
| T-10-03 | Repudiation | Phase audit evidence | mitigate | The inventory artifact records commands and migration decisions so later changes can be traced back to P10-01. |
</threat_model>

<verification>
Run the automated command in Task 1. No `src/**` or `test/**` implementation files should be modified in this plan.
</verification>

<success_criteria>
P10-01 is satisfied: all current store-boundary consumers are inventoried and classified before any implementation changes.
</success_criteria>

<output>
After completion, create `.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-01-SUMMARY.md`.
</output>

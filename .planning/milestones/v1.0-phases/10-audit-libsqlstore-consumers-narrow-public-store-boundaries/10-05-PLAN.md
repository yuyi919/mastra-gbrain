---
phase: 10-audit-libsqlstore-consumers-narrow-public-store-boundaries
plan: "05"
type: execute
wave: 5
depends_on:
  - "10-04"
files_modified:
  - src/search/hybrid.ts
  - src/tools/search.ts
  - src/tools/page.ts
  - src/tools/links.ts
  - src/tools/timeline.ts
  - src/tools/config.ts
  - src/tools/raw.ts
  - src/tools/list.ts
  - src/tools/import.ts
  - src/tools/ingest.ts
  - test/search/hybrid.test.ts
  - test/tools.test.ts
autonomous: true
requirements:
  - P10-02
  - P10-03
  - P10-04
  - P10-05
requirements_addressed:
  - P10-02
  - P10-03
  - P10-04
  - P10-05
must_haves:
  truths:
    - "Internal hybrid search execution uses `BrainStoreSearch` through an Effect runtime as the primary path."
    - "Tool internals do not introduce new Promise-shaped store contracts when a branch/runtime path is available."
    - "Public tool factories remain structurally compatible with `LibSQLStore` and `StoreProvider`."
    - "Any remaining Promise path is explicitly compatibility glue, not the internal dependency model."
  artifacts:
    - path: "src/search/hybrid.ts"
      provides: "Runtime-backed Effect-first hybrid search implementation"
      contains: "hybridSearchEffect"
    - path: "src/tools/search.ts"
      provides: "Search tool path delegating to Effect-first hybrid search"
      contains: "hybridSearch"
  key_links:
    - from: "src/tools/search.ts"
      to: "src/search/hybrid.ts"
      via: "runtime-backed hybridSearch"
      pattern: "hybridSearch\\("
---

<objective>
Move hybrid search and internal tool execution toward direct Effect runtime / branch services while preserving public tool factory compatibility.

Purpose: Apply D-23/D-24 to the tool/search lane. The old plan's "narrow Promise contracts" direction is superseded; internal code should prefer branch services and runtime execution, while Promise compatibility remains at the public factory boundary.
Output: Effect-first search/tool paths, tests proving branch/runtime injection, and no new tool-wide Promise compatibility layer.
</objective>

<locked_direction>
Non-negotiable direction for this plan:
- Do not add new Promise compatibility interfaces/layers for internal search or tool lanes.
- Internal implementations must use Effect runtime with branch services as the primary path.
- Promise wrappers are allowed only at public facade boundaries and must be treated as compatibility glue.
</locked_direction>

<execution_context>
@D:/workspace/@yuyi919/external/whole-ends-kneel/packages/yui-agent/packages/brain-mastra/.codex/get-shit-done/workflows/execute-plan.md
@D:/workspace/@yuyi919/external/whole-ends-kneel/packages/yui-agent/packages/brain-mastra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-INVENTORY.md
@.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-CONTEXT.md
@.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-PATTERNS.md
@AGENTS.md
@docs/effect/v4-systematic-guide.md
@docs/effect/v4-banned-patterns.md
@src/search/hybrid.ts
@src/tools/search.ts
@src/tools/page.ts
@src/tools/links.ts
@src/tools/timeline.ts
@src/tools/config.ts
@src/tools/raw.ts
@src/tools/list.ts
@src/agent/index.ts
@test/search/hybrid.test.ts
@test/tools.test.ts
</context>

<interfaces>
Do not create `src/tools/contracts.ts` merely to mirror `StoreProvider` as smaller Promise interfaces.

If a small runtime carrier type is needed for public compatibility wrappers, keep it local and capability-oriented, for example:
```typescript
export interface BrainStoreRuntimeCarrier {
  brainStore?: {
    runPromise<A, E>(effect: Effect.Effect<A, E, BrainStoreSearch>): Promise<A>;
  };
}
```
Prefer existing `BrainStoreSearch` / branch service contracts for actual internal implementation.
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Make hybrid search runtime-first</name>
  <files>src/search/hybrid.ts, test/search/hybrid.test.ts</files>
  <read_first>
    src/search/hybrid.ts
    test/search/hybrid.test.ts
    src/store/BrainStore.ts
    src/store/interface.ts
    docs/effect/v4-systematic-guide.md
  </read_first>
  <behavior>
    - `hybridSearchEffect` remains the canonical implementation requiring `BrainStoreSearch`.
    - `hybridSearch` uses `backend.brainStore.runPromise(hybridSearchEffect(...))` when a runtime is available.
    - Legacy Promise fallback, if retained for tests or external mocks, is named/commented as compatibility glue and not used as the model for new internal modules.
    - Tests prove the branch-only runtime path without casting narrow mocks to full `StoreProvider`.
  </behavior>
  <action>
    Review `src/search/hybrid.ts` after Plans 10-02 through 10-04. Keep `hybridSearchEffect` as the source of truth and make the Promise wrapper a thin runtime bridge. If the fallback test currently needs `as unknown as StoreProvider`, replace that with either a `ManagedRuntime` over `BrainStoreSearch` or a local compatibility type that is explicitly not exported as a general tool contract.

    Avoid adding a broad `HybridSearchPromiseStore` as the new internal model. If a named type is necessary, use a name that signals compatibility, such as `HybridSearchCompatBackend`, and keep the primary comments oriented around Effect runtime usage.
  </action>
  <verify>
    <automated>bun test test/search/hybrid.test.ts</automated>
    <automated>pwsh -NoProfile -Command "if (rg -n 'as unknown as StoreProvider|import type \\{ StoreProvider \\}' test/search/hybrid.test.ts src/search/hybrid.ts) { throw 'broad StoreProvider cast/import remains in hybrid search tests or implementation' }"</automated>
    <automated>rg -n "hybridSearchEffect|BrainStoreSearch|runPromise" src/search/hybrid.ts test/search/hybrid.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Hybrid search's internal path is Effect-first and branch-service backed; Promise fallback remains only as compatibility glue and no search test casts to `StoreProvider`.
  </acceptance_criteria>
  <done>Hybrid search follows the direct Effect runtime model.</done>
</task>

<task type="auto">
  <name>Task 2: Keep tool public factories while avoiding new Promise contracts</name>
  <files>src/tools/search.ts, src/tools/page.ts, src/tools/links.ts, src/tools/timeline.ts, src/tools/config.ts, src/tools/raw.ts, src/tools/list.ts, src/tools/import.ts, src/tools/ingest.ts, test/tools.test.ts</files>
  <read_first>
    src/tools/search.ts
    src/tools/page.ts
    src/tools/links.ts
    src/tools/timeline.ts
    src/tools/config.ts
    src/tools/raw.ts
    src/tools/list.ts
    src/tools/import.ts
    src/tools/ingest.ts
    src/agent/index.ts
    test/tools.test.ts
  </read_first>
  <action>
    Do not introduce `src/tools/contracts.ts` or a family of Promise-shaped tool store interfaces. Instead:
    - Keep public tool factory signatures compatible with `StoreProvider` where those factories are public boundary APIs.
    - For search, route through the Effect-first `hybridSearch` runtime path when the passed store has `brainStore`.
    - For ingest/import tools, keep the existing public `{ store, embedder }` workflow compatibility from Plan 10-02, but mark or structure it as a boundary wrapper so future internal callers can bypass it.
    - For page/link/timeline/config/raw/list tools, avoid changes unless a direct branch-service helper naturally emerges; do not churn public tool types purely for grep counts.

    If adding helper functions, they may be Effect-returning and service-specific, but they must not expose SQL/ORM internals or raw vector clients.
  </action>
  <verify>
    <automated>bun test test/tools.test.ts test/integration.test.ts test/search/hybrid.test.ts</automated>
    <automated>pwsh -NoProfile -Command "if (Test-Path src/tools/contracts.ts) { throw 'tool Promise contract layer should not be created for this corrected phase goal' }"</automated>
    <automated>pwsh -NoProfile -Command "if (rg -n 'PageToolsStore|LinksToolsStore|TimelineToolsStore|ConfigToolsStore|RawDataToolsStore|ListPagesStore|SearchToolStore|HybridSearchPromiseStore' src test) { throw 'old narrow Promise contract plan leaked into implementation' }"</automated>
  </verify>
  <acceptance_criteria>
    Tool public factory compatibility remains intact, search runs through the runtime-backed Effect path when available, and no new Promise compatibility layer is created for tools.
  </acceptance_criteria>
  <done>Tool/search work is aligned with direct Effect runtime usage rather than new Promise contracts.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public tool factory to internal implementation | Public callers still pass a facade-shaped store, but internal search behavior should execute branch services through the runtime. |
| compatibility wrapper to branch service | Promise wrappers must not become the source of truth. |
| tool input schemas to store calls | Existing zod schemas continue to validate tool input before store operations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-14 | Elevation of Privilege | tool/search dependency shape | mitigate | Prefer branch services and runtime execution over broad Promise store mocks for internal behavior. |
| T-10-15 | Tampering | zod tool boundaries | mitigate | Preserve existing `createTool` schemas and avoid unrelated behavior churn. |
| T-10-16 | Repudiation | compatibility wrappers | mitigate | Comment or name remaining Promise fallbacks as compatibility glue and test the Effect path directly. |
</threat_model>

<verification>
Run all task automated checks. If a tool needs a branch capability, add it to the existing branch service contract, not to `StoreProvider` or a new Promise mirror interface.
</verification>

<success_criteria>
P10-02 through P10-05 are satisfied for tool/search consumers: internal search behavior is Effect runtime backed, public tool compatibility remains, and the old Promise-contract plan is not implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/10-audit-libsqlstore-consumers-narrow-public-store-boundaries/10-05-SUMMARY.md`.
</output>

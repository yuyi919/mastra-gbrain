---
quick: "260425-q02"
mode: "quick"
description: "为项目接入 husky 与 lint-staged，在 pre-commit 中执行 check:fix（Biome）"
files_modified:
  - package.json
  - scripts/check-fix.mjs
  - .husky/.gitignore
  - .husky/pre-commit
  - .planning/quick/260425-q02-husky-lint-staged/260425-q02-PLAN.md
  - .planning/STATE.md
---

# Quick Task 260425-q02 Plan

## Objective

为仓库添加统一的 pre-commit 约束，使用 Husky 安装 Git hooks，用 lint-staged 只处理 staged 文件，并复用 `check:fix` 作为格式化与修复入口。

## Tasks

### Task 1: 配置 package.json 脚本与依赖
- files:
  - package.json
- action:
  - 增加 `husky`、`lint-staged` devDependencies。
  - 增加 `prepare` 与 `lint-staged` scripts。
  - 把 `check:fix` 调整为可接收 staged 文件参数的入口。
- verify:
  - `package.json` 中存在 Husky、lint-staged 与 `check:fix` 相关配置。
- done:
  - 包管理与任务入口配置完成。

### Task 2: 添加 hook 与 staged 文件执行入口
- files:
  - scripts/check-fix.mjs
  - .husky/.gitignore
  - .husky/pre-commit
- action:
  - 添加一个小包装脚本，让 `check:fix` 无参数时扫全仓，有参数时仅检查传入文件。
  - 添加 pre-commit hook，调用 `pnpm lint-staged`。
  - 忽略 Husky 生成的 `./.husky/_` 本地辅助文件，只跟踪项目需要的 hook 文件。
- verify:
  - `node scripts/check-fix.mjs package.json` 可以执行。
  - `.husky/pre-commit` 只负责触发 lint-staged。
- done:
  - pre-commit 入口与 staged 执行链路完成。

### Task 3: 记录 quick 任务结果
- files:
  - .planning/quick/260425-q02-husky-lint-staged/260425-q02-SUMMARY.md
  - .planning/STATE.md
- action:
  - 记录这次 quick task 的配置结果与局限。
- verify:
  - `STATE.md` 中新增 quick task 记录。
- done:
  - 本次改动有可追溯记录。

# Requirements

Current milestone requirements have been archived to `.planning/milestones/v1.0-REQUIREMENTS.md`.

Next milestone requirements are intentionally not drafted yet.

Immediate candidate scope:

- Split BrainStore into layered Context services and `makeLayer` composition.
- Narrow module dependencies to feature-specific layers for reuse and testability.
